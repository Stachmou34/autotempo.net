; Paramètres des notifications email.
; Copiez ce fichier en notif.ini et renseignez votre adresse.
;   cp notif.example.ini notif.ini
;   nano notif.ini
;
; email = adresse qui reçoit les alertes (plusieurs adresses séparées par une virgule)
; from  = adresse expéditrice (idéalement une adresse @autotempo.net pour éviter le spam)

email = "pereira.christophe34@hotmail.fr"
from  = "noreply@autotempo.net"
